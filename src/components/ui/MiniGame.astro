---
import DodgeGame from './games/DodgeGame.astro';
import FlappyBird from './games/FlappyBird.astro';
---

<div class="game-container">
  <div class="game-header">
    <h3>🎮 Mini Games</h3>
    <p class="game-instructions">Choose a game to play and have fun!</p>
  </div>

  <!-- Game Selector -->
  <div class="game-selector" id="gameSelector">
    <button class="game-card" data-game="dodge">
      <div class="game-card-icon">🎯</div>
      <h4>Beige Dodge</h4>
      <p>Dodge falling blocks and survive as long as you can!</p>
      <span class="game-card-action">Play Now →</span>
    </button>

    <button class="game-card" data-game="flappy">
      <div class="game-card-icon">🐦</div>
      <h4>Beige Bird</h4>
      <p>Tap to flap and navigate through pipes!</p>
      <span class="game-card-action">Play Now →</span>
    </button>
  </div>

  <!-- Game Display Area -->
  <div class="game-display" id="gameDisplay" style="display: none;">
    <button class="back-button" id="backButton">
      ← Back to Games
    </button>

    <div class="game-content" id="dodgeGame" style="display: none;">
      <DodgeGame />
    </div>

    <div class="game-content" id="flappyGame" style="display: none;">
      <FlappyBird />
    </div>
  </div>
</div>

<script>
  const gameSelector = document.getElementById('gameSelector')!;
  const gameDisplay = document.getElementById('gameDisplay')!;
  const backButton = document.getElementById('backButton')!;
  const dodgeGame = document.getElementById('dodgeGame')!;
  const flappyGame = document.getElementById('flappyGame')!;

  // Game selection
  document.querySelectorAll('.game-card').forEach(card => {
    card.addEventListener('click', () => {
      const gameName = (card as HTMLElement).dataset.game;

      gameSelector.style.display = 'none';
      gameDisplay.style.display = 'block';

      // Show selected game
      if (gameName === 'dodge') {
        dodgeGame.style.display = 'block';
        flappyGame.style.display = 'none';
      } else if (gameName === 'flappy') {
        flappyGame.style.display = 'block';
        dodgeGame.style.display = 'none';
      }
    });
  });

  // Back to selector
  backButton.addEventListener('click', () => {
    gameDisplay.style.display = 'none';
    gameSelector.style.display = 'grid';
    dodgeGame.style.display = 'none';
    flappyGame.style.display = 'none';
  });
</script>

<style>
  .game-container {
    margin: var(--space-12) 0;
    padding: var(--space-8);
    background: var(--color-bg-secondary);
    border-radius: var(--radius-lg);
    border: 2px solid var(--color-border);
  }

  .game-header {
    text-align: center;
    margin-bottom: var(--space-8);
  }

  .game-header h3 {
    font-size: var(--font-size-2xl);
    color: var(--color-accent-primary);
    margin-bottom: var(--space-3);
  }

  .game-instructions {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    margin: 0;
  }

  /* Game Selector */
  .game-selector {
    display: flex;
    gap: var(--space-4);
    overflow-x: auto;
    overflow-y: hidden;
    padding: var(--space-2) var(--space-1);
    margin: 0 auto;
    max-width: 100%;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: thin;
    scrollbar-color: var(--color-border) transparent;
  }

  .game-selector::-webkit-scrollbar {
    height: 6px;
  }

  .game-selector::-webkit-scrollbar-track {
    background: transparent;
    margin: 0 var(--space-4);
  }

  .game-selector::-webkit-scrollbar-thumb {
    background: var(--color-border);
    border-radius: var(--radius-full);
  }

  .game-selector::-webkit-scrollbar-thumb:hover {
    background: var(--color-accent-primary);
  }

  .game-card {
    background: var(--color-bg-primary);
    border: 2px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-5);
    text-align: center;
    cursor: pointer;
    transition: all var(--transition-base);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
    min-width: 180px;
    max-width: 200px;
    flex-shrink: 0;
    scroll-snap-align: start;
  }

  .game-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--color-accent-primary);
  }

  .game-card-icon {
    font-size: var(--font-size-4xl);
    margin-bottom: var(--space-1);
  }

  .game-card h4 {
    font-size: var(--font-size-base);
    color: var(--color-text-primary);
    margin: 0;
    font-weight: var(--font-weight-medium);
  }

  .game-card p {
    font-size: var(--font-size-xs);
    color: var(--color-text-secondary);
    margin: 0;
    flex-grow: 1;
    line-height: var(--line-height-tight);
  }

  .game-card-action {
    font-size: var(--font-size-xs);
    color: var(--color-accent-primary);
    font-weight: var(--font-weight-medium);
    margin-top: var(--space-1);
  }

  /* Game Display */
  .game-display {
    max-width: 600px;
    margin: 0 auto;
  }

  .back-button {
    background: var(--color-bg-primary);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-2) var(--space-4);
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    cursor: pointer;
    transition: all var(--transition-fast);
    margin-bottom: var(--space-6);
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
  }

  .back-button:hover {
    background: var(--color-bg-hover);
    color: var(--color-accent-primary);
    border-color: var(--color-accent-primary);
  }

  .game-content {
    margin-top: var(--space-4);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .game-container {
      padding: var(--space-4);
    }

    .game-header h3 {
      font-size: var(--font-size-xl);
    }

    .game-selector {
      gap: var(--space-3);
      padding: var(--space-2) 0;
    }

    .game-card {
      min-width: 160px;
      max-width: 180px;
      padding: var(--space-4);
    }

    .game-card-icon {
      font-size: var(--font-size-3xl);
    }
  }
</style>
